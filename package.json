{
  "private": true,
  "workspaces": [
    "demo",
    "gatsby-source-chec"
  ],
  "scripts": {
    "build": "yarn workspace demo build",
    "clean": "yarn workspace demo clean",
    "dev": "yarn workspace demo dev"
  }
}
